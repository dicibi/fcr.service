[program:gunicorn]
command=gunicorn app:app --workers 1 --bind 0.0.0.0:5000 --timeout 600

directory=/app

autostart=true
autorestart=true

stdout_logfile=/dev/stdout
stderr_logfile=/dev/stderr

environment=REDIS_OM_URL=%(ENV_REDIS_URL)s
